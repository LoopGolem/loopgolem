# Packaging

Planned artifacts: Windows x64/arm64 installer and portable archive; Linux amd64/arm64 .deb, AppImage and tar.gz.

Update discovery will use GitHub Releases. Automatic checks may be enabled, but download/install behavior remains user-controlled by default.
